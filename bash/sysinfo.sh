#!/bin/bash

#LAB 1 SYSTEM INFO QUIZ

echo 'This is a system info Script'

hostname -A #used for system’s fully-qualified domain name 

hostnamectl # used to  show system hostname , operating system name and version 

hostname -I # used to show ip address of the machine except 127
df -h / #this command used for show available space in root folder as a human readable form

